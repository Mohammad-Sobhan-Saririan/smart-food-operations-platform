# Smart Food Operations - configuration reference
# Copy Backend/.env.example to Backend/.env and Frontend/cafe-pwa/.env.example to Frontend/cafe-pwa/.env.local.
# This root file is documentation only; application-specific examples live with each app.

APP_NAME=Smart Food Operations
AUTH_MODE=local
NOTIFICATIONS_PROVIDER=none
LLM_ENABLED=false
